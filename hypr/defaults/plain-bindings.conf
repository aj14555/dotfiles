# Application bindings
bindd = SUPER, RETURN, Terminal, exec, uwsm-app -- xdg-terminal-exec --dir="$(sys-cmd-terminal-cwd)"
bindd = SUPER SHIFT, RETURN, Browser, exec, sys-launch-browser
bindd = SUPER SHIFT, F, File manager, exec, uwsm-app -- nautilus --new-window
bindd = SUPER ALT SHIFT, F, File manager (cwd), exec, uwsm-app -- nautilus --new-window "$(sys-cmd-terminal-cwd)"
bindd = SUPER SHIFT, B, Browser, exec, sys-launch-browser
bindd = SUPER SHIFT ALT, B, Browser (private), exec, sys-launch-browser --private
bindd = SUPER SHIFT, N, Editor, exec, sys-launch-editor

# Add extra bindings
# bindd = SUPER SHIFT, A, ChatGPT, exec, sys-launch-webapp "https://chatgpt.com"
# bindd = SUPER SHIFT, R, exec, alacritty -e ssh your-server

# Overwrite existing bindings, like putting Omarchy Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, Omarchy menu, exec, sys-menu
