# Menus
bindd = SUPER, SPACE, Launch apps, exec, sys-launch-walker
bindd = SUPER CTRL, E, Emoji picker, exec, sys-launch-walker -m symbols
bindd = SUPER CTRL, C, Capture menu, exec, sys-menu capture
bindd = SUPER CTRL, O, Toggle menu, exec, sys-menu toggle
bindd = SUPER CTRL, H, Hardware menu, exec, sys-menu hardware
bindd = SUPER ALT, SPACE, Omarchy menu, exec, sys-menu
bindd = SUPER SHIFT, code:201, Omarchy menu, exec, sys-menu
bindd = SUPER, ESCAPE, System menu, exec, sys-menu system
bindld = , XF86PowerOff, Power menu, exec, sys-menu system
bindd = SUPER, K, Show key bindings, exec, sys-menu-keybindings
bindd = , XF86Calculator, Calculator, exec, gnome-calculator

# Aesthetics
bindd = SUPER SHIFT, SPACE, Toggle top bar, exec, sys-toggle-waybar
bindd = SUPER CTRL, SPACE, Theme background menu, exec, sys-menu background
bindd = SUPER SHIFT CTRL, SPACE, Theme menu, exec, sys-menu theme
bindd = SUPER, BACKSPACE, Toggle window transparency, exec, sys-hyprland-window-transparency-toggle
bindd = SUPER SHIFT, BACKSPACE, Toggle window gaps, exec, sys-hyprland-window-gaps-toggle
bindd = SUPER CTRL, BACKSPACE, Toggle single-window square aspect, exec, sys-hyprland-window-single-square-aspect-toggle

# Notifications
bindd = SUPER, COMMA, Dismiss last notification, exec, makoctl dismiss
bindd = SUPER SHIFT, COMMA, Dismiss all notifications, exec, makoctl dismiss --all
bindd = SUPER CTRL, COMMA, Toggle silencing notifications, exec, sys-toggle-notification-silencing
bindd = SUPER ALT, COMMA, Invoke last notification, exec, makoctl invoke
bindd = SUPER SHIFT ALT, COMMA, Restore last notification, exec, makoctl restore

# Toggles
bindd = SUPER CTRL, I, Toggle locking on idle, exec, sys-toggle-idle
bindd = SUPER CTRL, N, Toggle nightlight, exec, sys-toggle-nightlight
bindd = SUPER CTRL, Delete, Toggle laptop display, exec, sys-hyprland-monitor-internal toggle
bindd = SUPER CTRL ALT, Delete, Toggle laptop display mirroring, exec, sys-hyprland-monitor-internal-mirror toggle
bindl = , switch:on:Lid Switch, exec, sys-hw-external-monitors && sys-hyprland-monitor-internal off
bindl = , switch:off:Lid Switch, exec, sys-hyprland-monitor-internal on

# Captures
bindd = , PRINT, Screenshot, exec, sys-capture-screenshot
bindd = ALT, PRINT, Screenrecording, exec, sys-menu screenrecord
bindd = SUPER, PRINT, Color picker, exec, pkill hyprpicker || hyprpicker -a
bindd = SUPER CTRL, PRINT, Extract text (OCR) from screenshot, exec, sys-capture-text-extraction

# File sharing
bindd = SUPER CTRL, S, Share, exec, sys-menu share

# Transcoding
bindd = SUPER CTRL, PERIOD, Transcode, exec, sys-transcode

# Reminders
bindd = SUPER CTRL, R, Set reminder, exec, sys-menu reminder-set
bindd = SUPER CTRL ALT, R, Show reminders, exec, sys-reminder show
bindd = SUPER SHIFT CTRL, R, Clear reminders, exec, sys-reminder clear

# Waybar-less information
bindd = SUPER CTRL ALT, T, Show time, exec, notify-send -u low "    $(date +"%A %H:%M  ·  %d %B %Y  ·  Week %V")"
bindd = SUPER CTRL ALT, B, Show battery remaining, exec, notify-send -u low "$(sys-battery-status)"
bindd = SUPER CTRL ALT, W, Show weather, exec, notify-send -u low "$(sys-weather-status)"

# Control panels
bindd = SUPER CTRL, A, Audio controls, exec, sys-launch-audio
bindd = SUPER CTRL, B, Bluetooth controls, exec, sys-launch-bluetooth
bindd = SUPER CTRL, W, Wifi controls, exec, sys-launch-wifi
bindd = SUPER CTRL, T, Activity, exec, sys-launch-tui btop

# Dictation
bindd = SUPER CTRL, X, Toggle dictation, exec, voxtype record toggle

bindd  = , F9, Start dictation (push-to-talk), exec, voxtype record start
binddr = , F9, Stop dictation (push-to-talk),  exec, voxtype record stop

# Zoom
bindd = SUPER CTRL, Z, Zoom in, exec, hyprctl keyword cursor:zoom_factor $(hyprctl getoption cursor:zoom_factor -j | jq '.float + 1')
bindd = SUPER CTRL ALT, Z, Reset zoom, exec, hyprctl keyword cursor:zoom_factor 1

# Lock system
bindd = SUPER CTRL, L, Lock system, exec, sys-system-lock
