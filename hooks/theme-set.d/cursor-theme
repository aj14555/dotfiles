#!/bin/bash

# Sync the active theme to Cursor after every theme change.
# omarchy-theme-set-vscode runs during theme changes; this hook
# keeps Cursor sync explicit in ~/.config and is the place to add custom logic.

omarchy-theme-set-vscode
